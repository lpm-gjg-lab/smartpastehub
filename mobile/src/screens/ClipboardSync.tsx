import React from 'react';
import { View, Text, Button } from 'react-native';

export default function ClipboardSync() {
  return (
    <View>
      <Text>Clipboard Sync</Text>
      <Button title="Scan QR Code" onPress={() => {}} />
    </View>
  );
}
